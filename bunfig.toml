[test]

# The deploy-flow integration test spawns real servers and uses
# isomorphic-git operations that can interfere with other git-based
# tests when run concurrently. Run it separately after the main suite.
exclude = ["test/integration/**"]
